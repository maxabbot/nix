---
# =============================================================================
# Productivity Role — Handlers
# =============================================================================

- name: Restart syncthing
  ansible.builtin.systemd:
    name: "syncthing@{{ local_user }}.service"
    state: restarted
  become: true
