# Nix build outputs
result
result-*

# Nix flake lock backup
flake.lock.bak

# direnv
.direnv

# Local secrets / credentials — never commit
github_pat
*.iso

# Claude Code per-machine permission approvals
.claude/settings.local.json
